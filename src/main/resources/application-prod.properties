# Production Configuration Override
# This file ensures OpenAPI/Swagger is completely disabled in production environments

# Disable OpenAPI documentation and UI
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false

# Additional security: disable OpenAPI endpoints entirely
management.endpoints.web.exposure.exclude=openapi,swagger-ui